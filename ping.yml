name: Website Pinger

on:
  schedule:
    - cron: "*/1 * * * *"   # every 5 minutes
  workflow_dispatch:        # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -L --max-time 15 https://stox.bedwars.workers.dev
